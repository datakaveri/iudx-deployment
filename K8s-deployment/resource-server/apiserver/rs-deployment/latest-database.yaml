apiVersion: apps/v1
kind: Deployment
metadata:
  name: rs-latest-database
  namespace: rs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rs-latest-database
  template:
    metadata:
      labels:
        app: rs-latest-database
      annotations:
        prometheus.io/port: "9000"
        prometheus.io/scrape: "true"
    spec:
      nodeSelector:
        node.kubernetes.io/instance-type: t3.medium
      containers:
      - name: rs-latest-database 
        image: ghcr.io/datakaveri/rs-depl:3.0-e194504
        resources:
          limits:
            cpu: 1000m
            memory: 1Gi
          requests:
            cpu: 800m
            memory: 800Mi
        volumeMounts:
          - name: rs-config-latest-database
            mountPath: /usr/share/app/secrets/one-verticle-configs
        ports:
          - name: prometheus
            containerPort: 9000
            protocol: TCP
          - name: hazelcast
            containerPort: 5701
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /metrics
            port: 9000
          initialDelaySeconds: 60
          periodSeconds: 60
          timeoutSeconds: 10
        envFrom:
          - configMapRef:
              name: rs-env
        env:
          - name: MY_POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
        command: ["/bin/bash"]
        args: ["-c","exec java -Xmx1024m -Dvertx.logger-delegate-factory-class-name=io.vertx.core.logging.Log4j2LogDelegateFactory  -jar ./fatjar.jar  --host $$MY_POD_IP -c secrets/one-verticle-configs/config-latest-database.json"]
      volumes:
        - name: rs-config-latest-database
          secret:
            secretName: rs-config-latest-database

---

apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: rs-latest-database
  namespace: rs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rs-latest-database
  minReplicas: 1
  maxReplicas: 7
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80

