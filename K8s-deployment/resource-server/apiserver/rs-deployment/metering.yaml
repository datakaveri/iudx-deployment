apiVersion: apps/v1
kind: Deployment
metadata:
  name: rs-metering
  namespace: rs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rs-metering
  template:
    metadata:
      labels:
        app: rs-metering
      annotations:
        prometheus.io/port: "9000"
        prometheus.io/scrape: "true"
    spec:
      nodeSelector:
        node.kubernetes.io/instance-type: t3.medium
      containers:
      - name: rs-metering 
        image: ghcr.io/datakaveri/rs-depl:3.0-e194504
        resources:
          limits:
            cpu: 700m
            memory: 1Gi
          requests:
            cpu: 500m
            memory: 800Mi
        volumeMounts:
          - name: rs-config-metering
            mountPath: /usr/share/app/secrets/one-verticle-configs
        ports:
          - name: prometheus
            containerPort: 9000
            protocol: TCP
          - name: hazelcast
            containerPort: 5701
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /metrics
            port: 9000
          initialDelaySeconds: 60
          periodSeconds: 60
          timeoutSeconds: 10
        envFrom:
          - configMapRef:
              name: rs-env
        env:
          - name: MY_POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
        command: ["/bin/bash"]
        args: ["-c","exec java -Xmx1024m -Dvertx.logger-delegate-factory-class-name=io.vertx.core.logging.Log4j2LogDelegateFactory  -jar ./fatjar.jar  --host $$MY_POD_IP -c secrets/one-verticle-configs/config-metering.json"]
      volumes:
        - name: rs-config-metering
          secret:
            secretName: rs-config-metering
---

apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: rs-metering
  namespace: rs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rs-metering
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80

