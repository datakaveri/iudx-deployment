apiVersion: apps/v1
kind: Deployment
metadata:
  name: cat-authenticator
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.authenticator.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.authenticator.labels.app }}
  template:
    metadata:
      labels:
        app: {{ .Values.authenticator.labels.app }}
      annotations:
        {{- include "math.annotation" . | nindent 8 }}
    spec:
      containers:
      - name: {{ .Values.authenticator.labels.app }}
        image: {{ .Values.image }}
        resources:
          limits:
            cpu: {{ .Values.authenticator.limits.cpu }}
            memory: {{ .Values.authenticator.limits.memory }}
          requests:
            cpu: {{ .Values.authenticator.requests.cpu }}
            memory: {{ .Values.authenticator.requests.memory }}
        volumeMounts:
          - name: cat-config-authenticator
            mountPath: {{ .Values.volumeConfigPath }}
        ports:
          - name: prometheus
            containerPort: {{ .Values.containerPorts.prometheus.port }}
            protocol: {{ .Values.containerPorts.prometheus.protocol }}
          - name: hazelcast
            containerPort: {{ .Values.containerPorts.hazelcast.port }}
            protocol: {{ .Values.containerPorts.hazelcast.protocol }}
        livenessProbe:
          httpGet:
            {{- include "math.livenessProbe.httpGet" . | nindent 12 }}
          initialDelaySeconds: {{ .Values.initialDelaySeconds }}
          periodSeconds: {{ .Values.periodSeconds }}
          timeoutSeconds: {{ .Values.timeoutSeconds }}
        envFrom:
          - configMapRef:
              name: {{ .Values.configMapRef }}
        env:
          - name: MY_POD_IP
            {{- include "math.env.myPodIp" . | nindent 12 }}
        command: [ {{ range .Values.authenticator.command }} {{ . | quote }} {{- end }} ]
        args: [ {{- range $index, $str := .Values.authenticator.args }} {{ if eq $index 0 }} {{ . | quote }} {{ else }} , {{  . | quote }} {{- end }}{{- end }} ]
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}    
      volumes:
        - name: cat-config-authenticator
          secret:
            secretName: cat-config-authenticator
