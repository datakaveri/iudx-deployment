apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cat-helm-test.fullname" . }}-test-connection"
  labels:
    {{- include "cat-helm-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cat-helm-test.fullname" . }}:{{ .Values.service.targetPort }}']
  restartPolicy: Never
