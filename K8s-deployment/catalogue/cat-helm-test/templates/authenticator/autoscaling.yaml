{{- if .Values.authenticator.autoscaling.enabled }}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: cat-authenticator
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "cat-helm-test.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cat-authenticator
  minReplicas: {{ .Values.authenticator.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.authenticator.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.authenticator.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.authenticator.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.authenticator.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.authenticator.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
