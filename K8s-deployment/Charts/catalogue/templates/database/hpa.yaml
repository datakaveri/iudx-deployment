{{- if .Values.catalogue.apiServer.autoscaling.enabled }}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: cat-api-server
  namespace: {{ .Values.namespace }}
  labels:
    app: cat-api-server
    {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: cat-api-server
  minReplicas: {{ .Values.catalogue.apiServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.catalogue.apiServer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.catalogue.apiServer.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.catalogue.apiServer.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.catalogue.apiServer.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.catalogue.apiServer.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
