{{- if and .Values.acl-apd.enabled .Values.acl-apd.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: acl-apd-server
  labels:
    app: acl-apd-server
    {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: acl-apd-server
  minReplicas: {{ .Values.acl-apd.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.acl-apd.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.acl-apd.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.apiServer.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.acl-apd.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.acl-apd.autoscaling.targetMemory  }}
    {{- end }}
{{- end }}
