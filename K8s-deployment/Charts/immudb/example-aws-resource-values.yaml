immudb:
  nodeSelector:
      node.kubernetes.io/instance-type: t3.medium

  resources:
    limits:
      cpu: 1000m
      memory: 1.2Gi
    requests: 
      cpu: 700m
      memory: 1.2Gi

  persistence:
    storageClass: "ebs-storage-class"

  install:
    createUsers: true