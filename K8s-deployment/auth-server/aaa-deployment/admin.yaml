apiVersion: apps/v1
kind: Deployment
metadata:
  name: aaa-admin
  namespace: aaa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aaa-admin
  template:
    metadata:
      labels:
        app: aaa-admin
      annotations:
        prometheus.io/port: "9000"
        prometheus.io/scrape: "true"
    spec:
      nodeSelector:
        node.kubernetes.io/instance-type: t3.medium
      containers:
      - name: aaa-admin 
        image: ghcr.io/datakaveri/aaa-depl:3.0-7afbdaf
        resources:
          limits:
            cpu: 800m
            memory: 1.2Gi
          requests:
            cpu: 600m
            memory: 1.2Gi
        volumeMounts:
          - name: aaa-config
            mountPath: /usr/share/app/secrets/all-verticles-configs
        ports:
          - name: prometheus
            containerPort: 9000
            protocol: TCP
          - name: hazelcast
            containerPort: 5701
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /metrics
            port: 9000
          initialDelaySeconds: 60
          periodSeconds: 60
          timeoutSeconds: 10
        envFrom:
          - configMapRef:
              name: aaa-env
        env:
          - name: MY_POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
        command: ["/bin/bash"]
        args: ["-c","exec java -Xmx1024m -Dvertx.logger-delegate-factory-class-name=io.vertx.core.logging.Log4j2LogDelegateFactory  -jar ./fatjar.jar  --host $$MY_POD_IP -c secrets/all-verticles-configs/config.json -m iudx.aaa.server.admin.AdminVerticle"]
      volumes:
        - name: aaa-config
          secret:
            secretName: aaa-config
---

apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: aaa-admin
  namespace: aaa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: aaa-admin
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80

