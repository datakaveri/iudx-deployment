name: Docker Image CI

on:
  registry_package:
    types: [published]

jobs:

  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Build the docker image
      run: |
        echo ${{ secrets.GITHUB_TOKEN }} > a.txt
        cat a.txt
        ls
        echo "${{ secrets.DOCKERPW }}" | docker login -u "tharak1242" --password-stdin
        pwd
