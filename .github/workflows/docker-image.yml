name: Docker Image CI

on:
  registry_package:
    types: [published]

jobs:

  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Build the docker image
      run: |
        ls
        pwd
        git fetch
        git checkout workflow
        echo "test message" > a.txt
        echo "1"
        git add a.txt
        echo "2"
        git commit -m "added test file"
        echo "3"
        git push --set-upstream origin workflow
