events {}

http {
    server {
        listen              443 ssl;
        server_name         localhost;
	ssl_certificate     /home/rs-server/https-certificate.pem;
	ssl_certificate_key /home/rs-server/https-key.pem;
	ssl_protocols       TLSv1.2;
	ssl_ciphers         HIGH:!aNULL:!MD5;
        location / {
            proxy_pass https://dev:8443;
        }
    }
}
