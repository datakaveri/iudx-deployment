version: '3.9'

services:
  file-server:
    deploy:
      resources:
        limits:
          pids: 500
          cpus: '0.85'
          memory: 1000M
        reservations:
          cpus: '0.02'   
          memory: 250M
    depends_on:
      - "zookeeper"