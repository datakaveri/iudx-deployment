version: "3.9"
networks:
  default:
    name: overlay-net
    driver: overlay
    external: true
services:
  createbuckets:
    image: minio/mc
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://tasks.minio:9000 $MINIO_ROOT_USER $MINIO_ROOT_PASSWORD;
      /usr/bin/mc admin info myminio;
      /usr/bin/mc mb myminio/clickhouse;
      /usr/bin/mc anonymous set public myminio/clickhouse;
      exit 0;
      "
    deploy:
      placement:
        constraints:
          - "node.labels.datalake-master-node==true"
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        window: 10s
    secrets:
      - source: minio.username
        target: minio_username
      - source: minio.password
        target: minio_password 
    environment:
      - MINIO_ROOT_USER=/run/secrets/minio_username
      - MINIO_ROOT_PASSWORD=/run/secrets/minio_password
    
    logging:
      driver: "json-file"
      options:
        max-file: "3"
        max-size: "5m"
        tag: "{\"name\":\"{{.Name}}\",\"id\":\"{{.ID}}\"}"

secrets:
  minio.username: 
    file: ./secrets/passwords/minio-username
  minio.password:
    file: ./secrets/passwords/minio-password