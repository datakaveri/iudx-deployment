version: '3.9'

services:
  cat:
    deploy:
      resources:
        limits:
          pids: 512
          cpus: '0.75'
          memory: 1024M
        reservations:
          cpus: '0.1'   
          memory: 256M
