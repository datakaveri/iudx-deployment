version: "3.9"

services:
  acl-apd:
    ports:
      - target: 8080  # API server HTTP port
        published: 8080
        mode: host
      - target: 9000 # metrics server port 
        published: 9000 
        mode: host 
